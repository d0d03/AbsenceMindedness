# AbscenceMindedness
Project for easier planing of annual leave

## Setup
1. Copy `application.properties.template` to `application.properties`
2. Set your own passwords for `db.file.password` and `db.user.password`
3. Run the app